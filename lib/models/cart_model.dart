class CartModel {
  String id, imageUrl, wisata, name, price;
  int item;
  CartModel({
    required this.id,
    required this.imageUrl,
    required this.wisata,
    required this.name,
    required this.price,
    required this.item,
  });
}

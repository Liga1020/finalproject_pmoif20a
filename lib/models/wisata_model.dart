class WisataModel {
  String id, imageUrl, wisata, name, description, price;
  int score;

  WisataModel({
    required this.id,
    required this.imageUrl,
    required this.wisata,
    required this.name,
    required this.description,
    required this.price,
    required this.score,

  });
}
